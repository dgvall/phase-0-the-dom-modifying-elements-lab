// Functional removing of element in the DOM
const main = document.getElementById("main")
main.remove()
// Functional creation of element in the DOM
const newHeader = document.createElement("h1")
newHeader.id = "victory"
// Giving value to the element in the DOM
newHeader.textContent = "Diego is the champion"
